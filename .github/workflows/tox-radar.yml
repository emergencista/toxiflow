name: Tox Radar

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:

jobs:
  run-tox-radar:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run Tox Radar
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TOX_RADAR_FEED_TIMEOUT_MS: ${{ secrets.TOX_RADAR_FEED_TIMEOUT_MS }}
          TOX_RADAR_FEED_MAX_RETRIES: ${{ secrets.TOX_RADAR_FEED_MAX_RETRIES }}
          TOX_RADAR_FEED_RETRY_DELAY_MS: ${{ secrets.TOX_RADAR_FEED_RETRY_DELAY_MS }}
        run: node tox-radar.js
